name: My Second Workflow
run-name: 😊😎 ${{ github.event.head_commit.message }}
on: 
  push:
    branches: 
      - main
  workflow_dispatch:
    
jobs:
  First-Job-In-Second-Workflow:
    runs-on: windows-latest
    steps:
      - run: hostname
    name: Show hostname of the VM
